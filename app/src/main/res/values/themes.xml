<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Compose uses MaterialTheme, so this theme is just for the Activity -->
    <style name="Theme.EventProAI" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary colors -->
        <item name="android:colorPrimary">@color/primary_blue</item>
        <item name="android:colorAccent">@color/accent_orange</item>
        
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/primary_blue</item>
    </style>
</resources>
